# Asimov Roadmap

current:
  version: "2.4.0"
  status: in_progress
  summary: "Enrich project.yaml with description, stack, conventions"
  deliverables:
    - "Add description, stack, conventions fields to project.yaml schema"
    - "build_system_prompt: inject new fields into AI context"
    - "cmd_init: generate project.yaml with new fields (TODO placeholders)"
    - "cmd_migrate: add missing fields to existing project.yaml"
    - "launch_ai: warn if description is empty or TODO"
    - "Update CHANGELOG"

previous:
  - version: "2.3.0"
    status: released
    summary: "Coding-standards as fourth core protocol"
  - version: "2.2.0"
    status: released
    summary: "Replace awk with yq/jq + migration command"
