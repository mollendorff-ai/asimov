# Asimov Roadmap

current:
  version: "2.3.0"
  status: released
  summary: "Coding-standards as fourth core protocol"
  deliverables:
    - "Add adapted coding-standards.json to protocols/ (bundled source)"
    - "cmd_init: install coding-standards.json alongside the other three"
    - "cmd_migrate: stop deleting coding-standards.json, add it if missing"
    - "build_system_prompt: include coding-standards rule in protocol injection"
    - "Orphan detection: add coding-standards.json to known files"
    - "Update README, CHANGELOG"

previous:
  - version: "2.2.0"
    status: released
    summary: "Replace awk with yq/jq + migration command"
